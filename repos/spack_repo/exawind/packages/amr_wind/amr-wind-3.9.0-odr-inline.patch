Fix ODR violations introduced by PR #1862 ("Remove more inline statements").
Non-template function swap_epsilon and explicit template specializations
determine_influenced_procs<TurbineFast> and determine_root_proc<TurbineFast>
are defined in headers included by multiple translation units and must be
marked inline to satisfy the One Definition Rule.

--- a/amr-wind/wind_energy/actuator/turbine/external/turbine_external_utils.H
+++ b/amr-wind/wind_energy/actuator/turbine/external/turbine_external_utils.H
@@ -14,7 +14,7 @@
 namespace amr_wind::actuator::external {

-void swap_epsilon(vs::Vector& eps)
+inline void swap_epsilon(vs::Vector& eps)
 {
     const auto x = eps.x();
     const auto y = eps.y();
--- a/amr-wind/wind_energy/actuator/turbine/fast/turbine_fast_ops.H
+++ b/amr-wind/wind_energy/actuator/turbine/fast/turbine_fast_ops.H
@@ -154,12 +154,12 @@

 template <>
-void determine_influenced_procs<TurbineFast>(
+inline void determine_influenced_procs<TurbineFast>(
     typename TurbineFast::DataType& data)
 {
     external_determine_influenced_procs<TurbineFast::DataType>(data);
 }

 template <>
-void determine_root_proc<TurbineFast>(
+inline void determine_root_proc<TurbineFast>(
     typename TurbineFast::DataType& data, amrex::Vector<int>& act_proc_count)
 {
     external_determine_root_proc<
